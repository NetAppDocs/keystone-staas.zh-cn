---
sidebar: sidebar 
permalink: concepts/service-levels.html 
keywords: service levels, service level, rate plan or service level 
summary: Keystone STaaS 版本 3 提供预定义服务级别的存储容量。Keystone管理的每个卷都与一个服务级别相关联。 
---
= Keystone 中的性能服务级别
:hardbreaks:
:allow-uri-read: 
:nofooter: 
:icons: font
:linkattrs: 
:imagesdir: ../media/


[role="lead"]
Keystone STaaS 为每种存储类型提供预定义的性能服务级别 (PSL)。每个 PSL 都指定了性能目标（IOPS、吞吐量和延迟）、支持的协议、平台和最小容量要求。查看可用的 PSL，为您的工作负载选择合适的服务级别，并规划您的订阅。

Keystone 订阅可以包括一个或多个存储类型的多个 PSL。每个 PSL 都有一个承诺容量，这是在订阅期限内为该服务级别计费的最低容量。


NOTE: AFX 订阅仅限于 AFX 存储类型，目前仅提供一个 PSL Extreme。



== 性能服务级别实例

在您的环境中，性能服务级别 (PSL) 可以有多个实例，称为性能服务级别实例 (PSLI)。每个 PSLI 都是分配给该性能服务级别的独立存储系统，承诺容量按每个 PSLI 进行测量。

* 对于统一或块优化的存储，PSLI 是由 HA 对中的两个存储控制器与内部或外部连接的存储介质组成的存储阵列。
* 对于 AFX 存储，PSLI 是单个 AFX 存储控制器。AFX 使用分解式架构，其中控制器（计算）和驱动器盘架（存储）是独立的，可以单独扩展。IOPS 和吞吐量目标适用于每个控制器；承诺容量在 AFX 集群级别测量，其中包括集群中的所有控制器和驱动器盘架。


IOPS、吞吐量 (GBps) 和延迟 (ms) 等性能指标是根据 PSLI 来衡量的。


TIP: NetApp Keystone STaaS 性能服务级别的详细服务说明可供查阅 https://www.netapp.com/services/keystone/terms-and-conditions/["此处"^] 。

下面将介绍支持的数据存储类型（统一、块优化、AFX、对象和云服务）的基本性能服务级别：



== 统一存储的性能服务级别

|===


| *性能服务级别*（每个性能服务级别实例 [HA 对] 的所有规格） | * 至尊 * | * 高级版 * | * 标准 * | * 值 * 


| *支持的协议* 4+| FC、iSCSI、NFS、NFSv4/RDMA、NVMe/FC、NVMe/TCP、SMB、S3 


| *工作负载类型示例* | 人工智能/机器学习、高性能计算、内存数据库 | 分析、EDA、OLTP | OLAP、物联网、容器 | 备份、存档 


| *目标延迟为90^第^个百位点* | ≤1毫秒 | ≤1毫秒 | ≤4毫秒 | >4毫秒 


| *目标最大 IOPS*^1^ | 1M | 550 K | 25 万 | 不适用 


| *目标最大 GBps*^1^ | 40 | 20 | 10 | 不适用 


| * 平台 * | AFF A 系列 | AFF A 系列 | AFF C 系列 | FAS 


| *最小已提交容量* | 50 TiB | 50 TiB | 100 TiB | 100 TiB 


| *增量承诺容量增加* 4+| 25 TiB 


| *已提交和计量容量类型* 4+| 逻辑或物理 
|===


== 块优化存储的性能服务级别

|===


| *性能服务级别*（每个性能服务级别实例 [HA 对] 的所有规格） | * 至尊 * | * 高级版 * | * 标准 * 


| *支持的协议* 3+| NVMe/TCP、NVMe/FC、FC、iSCSI 


| *工作负载类型示例* 3+| SAP HANA、Oracle、MS SQL Server、EPIC 


| *目标延迟为90^第^个百位点* | ≤1毫秒 | ≤1毫秒 | ≤4毫秒 


| *目标最大 IOPS*^1^ | 850 K | 450 K | 120K 


| *目标最大 GBps* | 65 | 25. | 6 


| * 平台 * | ASA A 系列 | ASA A 系列 | ASA C 系列 


| *最小已提交容量* | 50 TiB | 50 TiB | 100 TiB 


| *增量承诺容量增加* 3+| 25TiB 


| *已提交和计量容量类型* 3+| 逻辑或物理 
|===


== AFF 存储的性能服务级别

|===


| *性能服务级别*（每个性能服务级别实例的所有规格 [一个 AFF 存储控制器]） | * 至尊 * 


| *支持的协议* | NFSv3、NFSv4/RDMA、pNFS、SMB、S3 


| *工作负载类型示例* | 人工智能/机器学习、高性能计算、内存数据库 


| *目标延迟为90^第^个百位点* | ≤1毫秒 


| *目标最大 IOPS*^1^ | 625K 


| *目标最大 GBps* | 20 


| * 平台 * | AFX 


| *每个 AFX 集群的最小承诺容量*^2^ | 200 TiB 


| *增量承诺容量增加* | 100 TiB 


| *已提交和计量容量类型* | 物理 
|===
[NOTE]
====
* ^1^ 互斥目标。实际性能可能因各种因素而异，包括操作系统版本、硬件、工作负载类型和并发操作数量。
* ^2^在 AFX 集群中，性能目标适用于每个存储控制器。添加到集群的每个控制器贡献相同的 IOPS 和吞吐量。承诺容量和增量增加适用于整个集群的总驱动器盘架容量。


====


== 统一、块优化和 AFX 存储的性能测量条件

基本性能服务级别指标取决于以下条件：

* 性能服务级别支持 ONTAP 9.8 及更高版本。
* 对于统一存储，
+
** *IOPS*：对于带有 NFS 的 ONTAP 9.16.1，每个性能级别实例支持随机访问，读取率为 70%，写入率为 30%，块大小为 8 KB，延迟为 1 毫秒（标准为 4 毫秒）。
** *吞吐量*：对于带有 NFS 的 ONTAP 9.16.1，每个性能级别实例都支持 100% 读取和 32 KB 块大小的顺序访问。


* 对于块优化存储，
+
** *IOPS*：对于带有 FCP 的 ONTAP 9.16.1，每个性能级别实例支持读取率为 70%、写入率为 30%、块大小为 8 KB 且延迟为 1 毫秒的随机访问。
** *吞吐量*：对于带有 FCP 的 ONTAP 9.16.1，每个性能级别实例都支持 100% 读取和 64 KB 块大小的顺序访问。


* 对于 AFX 存储，
+
** *IOPS*：对于使用 NFSv4 的 ONTAP 9.18.1，每个性能级别实例支持随机访问，读写比例为 70% 读取和 30% 写入，块大小为 4 KB。
** *吞吐量*：对于使用 NFSv4 的 ONTAP 9.18.1，每个性能级别实例都支持 100% 读取和 64 KB 块大小的顺序访问。


* 延迟不包括以下内容：
+
** 应用程序或主机延迟
** 与控制器端口之间的客户网络延迟
** 在使用FabricPool 的情况下、与向对象存储传输数据相关的开销


* 延迟值不适用于MetroCluster 写入操作。这些写入操作取决于远程系统的距离。
* 只有当层策略设置为"无"且云中没有任何块时、_Expected IOPs_才会针对FabricPool。_expected IOPs_适用于不属于SnapMirror同步关系的卷。




== 对象存储的性能服务级别

|===


| *性能服务水平* | * 标准 * | * 值 * 


| *支持的协议* 2+| S3 


| * 平台 * | SGF6112 | SG6160 


| *每份订单的最低承诺容量* | 200 TiB | 500 TiB 


| *增量承诺容量增加* | 25 TiB | 100 TiB 


| *已提交和计量容量类型* 2+| 物理 
|===


== 云存储

|===


| *性能服务水平* | *Cloud Volumes ONTAP* 


| *支持的协议* | NFS、CIFS、iSCSI 和 S3（仅限 AWS 和 Azure） 


| * 平台 * | Cloud Volumes ONTAP 


| *每份订单的最低承诺容量* | 4 TiB 


| *增量承诺容量增加* | 1 TiB 


| *已提交和计量容量类型* | 逻辑 
|===
[NOTE]
====
* 计算、存储、网络等云原生服务由云提供商开具发票。
* 这些服务依赖于云存储和计算特性。


====
* 相关信息 *

* link:../concepts/supported-storage-capacity.html["Keystone 中的存储容量类型"]
* link:..//concepts/metrics.html["Keystone服务中使用的指标和定义"]
* link:../concepts/pricing.html["Keystone定价"]

